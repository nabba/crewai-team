crewai>=1.11.0
langchain-anthropic>=1.4.0
anthropic==0.97.0
fastapi>=0.135.0
python-multipart>=0.0.9
uvicorn[standard]>=0.42.0
uvloop>=0.19.0
pydantic-settings>=2.10.0
apscheduler>=3.10.0
docker>=7.0.0
chromadb>=1.1.0,<2.0.0          # crewai 1.14.x pins chromadb~=1.1.0; previous >=1.5.8 was unsatisfiable
sentence-transformers>=5.0.0
trafilatura>=2.0.0
youtube-transcript-api>=1.2.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0
firebase-admin>=6.5.0
pypdf>=4.0,<6
python-docx>=1.0
python-pptx>=0.6.21
openpyxl>=3.1
# Google Workspace native integration (Gmail / Calendar / Docs / Sheets / Slides)
# via OAuth installed-app flow. google-auth + google-api-core are pulled in
# transitively by firebase-admin already; the next three add the discovery
# client + OAuth helpers.
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.1
# Web Push (PWA notifications) — VAPID-signed messages to browser endpoints.
pywebpush>=1.14
# Discord connector — second messaging surface alongside Signal.
discord.py>=2.3
Pillow>=10.0
litellm>=1.50.0
# mem0ai[nlp] — pulls in spaCy (~50 MB) + dependencies. The NLP extra
# powers two retrieval-quality features inside mem0:
#   1. BM25 lemmatization (mem0/utils/lemmatization.py) — better recall
#      on inflected words, populated as `payload.text_lemmatized`.
#   2. Named-entity + noun-compound extraction (entity_extraction.py)
#      — populated as `payload.entities` for metadata-aware filtering.
# Without [nlp], both fall back to raw tokens / empty entities and a
# WARNING fires on every memory write. The en_core_web_sm model still
# needs a separate `python -m spacy download` step (see Dockerfile).
mem0ai[nlp]>=2.0.1,<3.0.0
# ollama python client — required for mem0's native ollama provider.
# We use it (not litellm) for local-model fact extraction because mem0's
# litellm provider has an unconditional supports_function_calling()
# pre-flight gate that rejects every Ollama model regardless of whether
# the actual call uses tools (mem0/llms/litellm.py:70).
# See app/memory/mem0_manager.py for the provider-selection logic.
ollama>=0.4.0
# Google Earth Engine — satellite-imagery analysis surfaced to the
# coding crew via app/tools/gee_tool.py. Requires
# GOOGLE_APPLICATION_CREDENTIALS pointing at a service-account JSON
# with "Earth Engine Resource Writer" + "Service Usage Consumer"
# IAM roles. See .env.example for the setup walkthrough.
earthengine-api>=1.0.0
langchain-text-splitters>=0.2.0
python-pptx>=0.6.21
html2text>=2024.2.0
psycopg2-binary>=2.9.9
prometheus-client>=0.20.0  # Metric primitives consumed by Prometheus / Alertmanager
prometheus-fastapi-instrumentator>=7.0.0  # Auto-emit http_requests_total + duration histogram on /metrics
reportlab>=4.0
firecrawl-py>=1.0
neo4j>=5.0
mcp>=1.0  # Model Context Protocol — SSE server + client for cross-agent context
playwright>=1.45  # Headless browser automation for JS-heavy pages (T1-4)
yfinance>=0.2.0  # Free Yahoo Finance API — stock data, financials, SEC CIK mapping
bandit>=1.7.5  # Forge: static security scanner for agent-generated tools
# ShinkaEvolve installed separately (httpx version conflict with crewai)
# Install via: pip install --no-deps shinka-evolve @ git+https://github.com/SakanaAI/ShinkaEvolve.git
# langchain-neo4j removed — causes resolution-too-deep with crewai's langchain
# composio-crewai removed — deep dependency tree conflicts with crewai resolver
