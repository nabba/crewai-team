# Library adoption proposal — OpenRouter Unified Tool-Calling Web Search

> Auto-generated by `app.library_radar`. Filtered from `scope_tech_radar` ChromaDB on category=`tools`.
> Signature: `9f3c17254a0f`

## Summary

OpenRouter now provides the ability for tool-calling models to autonomously search the web and fetch page content using multiple selectable engines.

## Tech-radar suggested action

Evaluate replacing custom search tools with OpenRouter's native web search/fetch capabilities to simplify the agent tool stack.

## Candidate package name(s)

`openrouter`, `unified`, `tool-calling`, `search`, `provides`, `ability`

The operator vets the actual PyPI name; the radar's title/summary
is heuristically tokenised here as a starting point.

## Adoption checklist

- [ ] Confirm the PyPI name (or the GitHub repo if the package isn't on PyPI)
- [ ] Verify the license is compatible with the project license
- [ ] Check the package's release cadence + maintainer signal
- [ ] Trial in a coding-session worktree against a small benchmark
- [ ] If trial passes, file a change-request to add the pin to
      `requirements.txt` (standard /cp/changes gate)

## Operator action

- **Adopt**: file a change-request for the `requirements.txt` edit.
- **Defer**: leave this file in place; the dedup-by-signature
  guard prevents re-emitting the same discovery.
- **Reject**: delete this file. (The next radar pass may re-emit
  if the discovery resurfaces — that's a feature, not a bug, since
  it surfaces persistent operator-relevant signal.)
